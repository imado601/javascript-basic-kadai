//引数numberで値を受け取る関数valueを定義する
const value = (number) => {

    //受け取った値を二乗して計算結果をresultに返す
    var result = number ** 2;
}

//関数を呼び出し、引数として6を渡し、計算結果をresultに代入する
var result = value(6);
console.log(result);